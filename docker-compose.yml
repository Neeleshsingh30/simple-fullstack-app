services:

  backend:
    build: ./backend
    container_name: simple-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: simple-frontend
    ports:
      - "8080:80"
    depends_on:
      - backend